@startuml
title UC-01 Register / Login / Logout
start
:Open site;
if (User chooses Register?) then (yes)
  :Open registration form;
  :Fill fields (email, password, name...);
  :Submit;
  if (Validation OK?) then (yes)
    :Create account;
    :Send verification/email;
    stop
  else (no)
    :Show errors;
    stop
  endif
else (no)
  if (User chooses Login?) then (yes)
    :Open login form;
    :Enter credentials;
    :Submit;
    if (Auth OK?) then (yes)
      :Create session / token;
      :Redirect to dashboard;
      stop
    else (no)
      :Show login error;
      stop
    endif
  else (no)
    :Browse as guest;
    stop
  endif
endif
@enduml
