@startuml
title UC-13 View Reports / Analytics
start
:User (Instructor/Admin) opens analytics dashboard;
:Select report type (engagement, revenue, submissions...);
partition "System" {
  :Query analytics DB / data warehouse;
  if (Cached report available?) then (yes)
    :Return cached results;
  else (no)
    :Run aggregation queries;
    :Cache results for future;
  endif
}
:Render charts / tables / export options;
if (User exports?) then (yes)
  :Generate CSV / PDF;
  :Provide download link;
endif
stop
@enduml
