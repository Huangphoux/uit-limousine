@startuml
title UC-11 Create Course (Instructor)
start
:Instructor clicks "Create Course";
:Fill metadata (title, price, language...);
:Add modules, lessons, assignments;
:Save as Draft or Submit for Review;
partition "System" {
  if (Save as Draft?) then (yes)
    :Persist Course (status=DRAFT);
    :Return to instructor dashboard;
    stop
  else (submit for review)
    :Persist Course (status=PENDING_REVIEW);
    :Create InstructorApplication entry (if needed);
    :Notify Admins for approval;
    stop
  endif
}
@enduml
