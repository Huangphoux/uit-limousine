@startuml
title UC-14 Issue Certificate (System / Admin)
start
:Trigger (completion detected or admin triggers);
partition "System" {
  :Check user's completion criteria (progress, assignments, pass score);
  if (Eligible?) then (yes)
    :Generate certificate record & verificationCode;
    :Create certificate file or URL;
    :Persist Certificate;
    :Send certificate notification/email to user;
  else (no)
    :Log reason / inform admin;
  endif
}
stop
@enduml
