@startuml
title UC-09 Notification (Create & Deliver)
start
:Event triggers notification (e.g., grade posted, enrollment);
partition "System" {
  :Create Notification record (channel: IN_APP, EMAIL?);
  fork
    :Store in-app notification;
  fork again
    if (EMAIL required?) then (yes)
      :Queue/send email via mail service;
    else (no)
    endif
  end fork
}
:User receives notification (in-app / email);
stop
@enduml
