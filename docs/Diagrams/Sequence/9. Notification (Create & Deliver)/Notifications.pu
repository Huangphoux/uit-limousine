@startuml
title UC-09 Notification (Sequence)
participant System
participant NotificationService
participant DB
participant EmailService
participant User

System -> NotificationService: createNotification(event, userId, channel)
NotificationService -> DB: INSERT notification
DB --> NotificationService: saved
par Deliver channels
NotificationService -> User: push in-app
alt channel includes EMAIL
NotificationService -> EmailService: sendEmail(userEmail, content)
EmailService --> User: deliver email
end
end
NotificationService --> System: ack
@enduml
