#set heading(offset: 2)

= Nội dung bản tự đánh giá cá nhân
- Công việc cá nhân đã trực tiếp thực hiện trong nhóm: Cấu hình docker-compose
- Phần kiến thức cá nhân nắm rõ nhất trong bài thực hành: Cách docker-compose hoạt động, cấu trúc một file docker-compose, các tính năng liên quan đến docker-compose như build, pull, env_file, environment, port, volumns, ..., một số câu lệnh liên quan: docker compose up, docker compose down.
- Một khó khăn kỹ thuật đã gặp trong quá trình thực hiện và cách giải quyết: Trong quá trình thực hiện có xuất hiện vấn đề về hết dung lượng để build image và chạy container. Quyết định giải quyết là cài lại docker với thư mục build image trên ổ đĩa khác ngoài ổ C.
- Tự đánh giá mức độ đóng góp của bản thân trong nhóm (%): Mức độ đóng góp khoảng 6%. Phần công việc chỉ tập trung vào cấu hình docker-compose sao cho cả client và server đều hoạt động sau khi Dockerfile của mỗi thành phần được xây dựng. Về mặt kỹ thuật chỉ liên quan đến những tính năng và câu lệnh của docker compose.